# We need to keep the build directories themselves, but ignore
# Any compiled files inside them. Everyone builds their own.
#DrupalCon/build/iphone/*
#DrupalCon/build/android/*

# Compiled files. We're copying this line from KitchenSink's repository.
*.pyc
